// Re-export all JSDoc typedefs for central import reference

/**
 * @typedef {import('./user').User} User
 * @typedef {import('./blog').Blog } Blog
 * @typedef {import('./contact').Contact} Contact
 * @typedef {import('./order').Order} Order
 * @typedef {import('./newsletter').NewsletterSubscription} NewsletterSubscription
 * @typedef {import('./package').Package} Package
 * @typedef {import('./adminLog').AdminLog} AdminLog
 * @typedef {import('./passwordReset').PasswordReset} PasswordReset
 * @typedef {import('./event').Event} Event
 * @typedef {import('./registration').Registration} Registration
 */

module.exports = {};